(()=>{'use strict';const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)],body=document.body,root=document.documentElement,reduce=matchMedia('(prefers-reduced-motion: reduce)').matches,fine=matchMedia('(pointer:fine)').matches;const themes=[{id:'clyde',name:'Clyde',desc:'Warm editorial · clean portfolio',bg:'#f4efe6',a:'#0b7f7b',a2:'#d95f42',num:'01 / 05'},{id:'beckham',name:'Beckham',desc:'Dark cinematic · fullscreen portfolio',bg:'#090b0d',a:'#e8b94e',a2:'#ef6557',num:'02 / 05'},{id:'schmidt',name:'Schmidt',desc:'Split-screen · modern technology',bg:'#eaf1f6',a:'#3fc2ff',a2:'#7b68ee',num:'03 / 05'},{id:'breed2',name:'Breed2',desc:'Bright · vibrant · personal profile',bg:'#f7f5ff',a:'#7b35e8',a2:'#f0446d',num:'04 / 05'},{id:'grunt',name:'Grunt',desc:'Minimal · monochrome · sharp',bg:'#f1f1ed',a:'#111111',a2:'#777777',num:'05 / 05'}];let current='clyde';try{current=localStorage.getItem('amjid-profile-theme')||'clyde'}catch(e){}if(!themes.some(t=>t.id===current))current='clyde';body.dataset.theme=current;const sw=$('#themeSwitcher'),trigger=$('#themeTrigger'),panel=$('#themePanel'),options=$('#themeOptions'),index=$('#themeIndex');const render=()=>{options.innerHTML=themes.map(t=>`<button class="theme-option ${t.id===current?'active':''}" data-theme="${t.id}" aria-pressed="${t.id===current}"><span class="theme-preview" style="--pbg:${t.bg};--pa:${t.a};--pa2:${t.a2};--ps:${t.id==='beckham'?'#161a1c':'#fff'}"></span><span><b>${t.name}</b><small>${t.desc}</small></span><span class="theme-check">${t.id===current?'✓':''}</span></button>`).join('');index.textContent=themes.find(t=>t.id===current).num};const applyTheme=id=>{if(!themes.some(t=>t.id===id)||id===current)return;current=id;body.classList.add('theme-changing');body.dataset.theme=id;render();try{localStorage.setItem('amjid-profile-theme',id)}catch(e){}setTimeout(()=>body.classList.remove('theme-changing'),850)};render();trigger.addEventListener('click',e=>{e.stopPropagation();const open=sw.classList.toggle('open');trigger.setAttribute('aria-expanded',open)});options.addEventListener('click',e=>{const b=e.target.closest('[data-theme]');if(!b)return;applyTheme(b.dataset.theme);sw.classList.remove('open');trigger.setAttribute('aria-expanded','false')});document.addEventListener('click',e=>{if(!sw.contains(e.target)){sw.classList.remove('open');trigger.setAttribute('aria-expanded','false')}});document.addEventListener('keydown',e=>{if(e.key==='Escape'){sw.classList.remove('open');trigger.setAttribute('aria-expanded','false')}});
const menu=$('#menuBtn'),mobile=$('#mobileNav');if(menu&&mobile){menu.addEventListener('click',()=>{const open=mobile.classList.toggle('open');menu.setAttribute('aria-expanded',open)});$$('a',mobile).forEach(a=>a.addEventListener('click',()=>{mobile.classList.remove('open');menu.setAttribute('aria-expanded','false')}))}
const header=$('#header'),progress=$('#progress');const scroll=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max?scrollY/max*100:0)+'%';header.classList.toggle('scrolled',scrollY>35)};addEventListener('scroll',scroll,{passive:true});scroll();
const reveals=$$('.reveal');if(reduce)reveals.forEach(x=>x.classList.add('visible'));else{reveals.forEach((x,i)=>x.style.transitionDelay=(i%5)*80+'ms');if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -8% 0px'});reveals.forEach(x=>io.observe(x))}else reveals.forEach(x=>x.classList.add('visible'))}
const counters=$$('[data-count]');if('IntersectionObserver'in window){const co=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=+el.dataset.count,suf=el.dataset.suffix||'';if(reduce){el.textContent=target+suf}else{let st=performance.now();const tick=n=>{const p=Math.min(1,(n-st)/1500),q=1-Math.pow(1-p,3);el.textContent=Math.round(target*q)+suf;if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)}co.unobserve(el)}),{threshold:.7});counters.forEach(x=>co.observe(x))}
const sections=$$('.section-anchor'),nav=$$('.nav a');if('IntersectionObserver'in window){const so=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)nav.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}),{rootMargin:'-40% 0px -50% 0px'});sections.forEach(s=>so.observe(s))}
if(fine&&!reduce){const tilt=(el,amount=8)=>{let raf=0,tx=0,ty=0,cx=0,cy=0;el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();tx=(e.clientX-r.left)/r.width-.5;ty=(e.clientY-r.top)/r.height-.5;if(!raf)raf=requestAnimationFrame(function loop(){cx+=(tx-cx)*.09;cy+=(ty-cy)*.09;el.style.transform=`perspective(1400px) rotateX(${cy*-amount}deg) rotateY(${cx*amount}deg) translateZ(10px)`;if(Math.abs(tx-cx)>.001||Math.abs(ty-cy)>.001)raf=requestAnimationFrame(loop);else raf=0})});el.addEventListener('pointerleave',()=>{tx=ty=0;el.style.transform=''})};$$('.tilt').forEach(x=>tilt(x));$$('.metric,.award-card,.company-card,.digital-card').forEach(x=>tilt(x,3));document.addEventListener('pointermove',e=>{root.style.setProperty('--mx',((e.clientX/innerWidth)-.5).toFixed(3));root.style.setProperty('--my',((e.clientY/innerHeight)-.5).toFixed(3))},{passive:true})}
const filters=$$('.company-tabs button'),cards=$$('.company-card');filters.forEach(b=>b.addEventListener('click',()=>{const val=b.dataset.company;filters.forEach(x=>x.classList.toggle('active',x===b));cards.forEach((c,i)=>{const show=val==='all'||c.dataset.company===val;c.classList.toggle('is-hidden',!show);if(show&&!reduce)c.animate([{opacity:.1,transform:'translateY(18px)'},{opacity:1,transform:'none'}],{duration:600,delay:i*70,easing:'cubic-bezier(.16,1,.3,1)',fill:'both'})})}));const year=$('#year');if(year)year.textContent=new Date().getFullYear();
})();