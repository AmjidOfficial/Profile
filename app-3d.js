(()=>{'use strict';const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const fine=matchMedia('(pointer:fine)').matches;const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];const root=document.documentElement;const header=$('#site-nav');const progress=$('#scroll-progress');const menu=$('.menu');const mobile=$('.mobile-menu');const style=document.createElement('style');style.textContent='.nav-links svg,.btn svg,.icon-badge svg,.award-icon svg,.project-icon svg{width:18px;height:18px;display:block;flex:0 0 auto}.nav-links svg{width:14px;height:14px}.icon-badge svg,.award-icon svg,.project-icon svg{width:22px;height:22px}.btn svg{width:16px;height:16px}.skills{display:flex;flex-wrap:wrap;gap:8px}.skills span{padding:7px 9px;border:1px solid rgba(255,255,255,.09);border-radius:999px;color:#aeb2c7;font-size:.65rem;background:rgba(255,255,255,.03)}';document.head.appendChild(style);
const setVisible=(el,on)=>el&&el.classList.toggle('visible',on);if(menu&&mobile){menu.addEventListener('click',()=>{const open=mobile.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});$$('a',mobile).forEach(a=>a.addEventListener('click',()=>{mobile.classList.remove('open');menu.setAttribute('aria-expanded','false')}));document.addEventListener('click',e=>{if(!mobile.contains(e.target)&&!menu.contains(e.target)){mobile.classList.remove('open');menu.setAttribute('aria-expanded','false')}});document.addEventListener('keydown',e=>{if(e.key==='Escape'){mobile.classList.remove('open');menu.setAttribute('aria-expanded','false')}})}
const reveals=$$('.reveal');reveals.forEach((el,i)=>el.style.setProperty('--delay',`${Math.min((i%6)*70,350)}ms`));if('IntersectionObserver'in window&&!reduce){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){setVisible(e.target,true);const track=e.target.closest('.career-track');if(track)track.classList.add('active');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -9% 0px'});reveals.forEach(el=>io.observe(el))}else reveals.forEach(el=>setVisible(el,true));
const counters=$$('.counter');const counterObserver='IntersectionObserver'in window?new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=Number(el.dataset.target||0),suffix=el.dataset.suffix||'';if(reduce)el.textContent=target+suffix;else{const start=performance.now(),duration=1900;const tick=now=>{const p=Math.min(1,(now-start)/duration),v=1-Math.pow(1-p,4);el.textContent=Math.round(target*v)+suffix;if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)}counterObserver.unobserve(el)}),{threshold:.65}):null;counters.forEach(c=>counterObserver?counterObserver.observe(c):c.textContent=(c.dataset.target||0)+(c.dataset.suffix||''));
const sections=$$('.page-section');const nav=$$('.nav-links .nav-tab');if('IntersectionObserver'in window){const so=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const id=e.target.id;nav.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+id))}),{rootMargin:'-42% 0px -48% 0px'});sections.forEach(s=>so.observe(s))}
const scrollUpdate=()=>{const max=document.documentElement.scrollHeight-innerHeight;const pct=max>0?(scrollY/max)*100:0;if(progress)progress.style.width=`${pct}%`;if(header)header.classList.toggle('scrolled',scrollY>22)};addEventListener('scroll',scrollUpdate,{passive:true});scrollUpdate();
const card=$('.profile-card'),stage=$('.hero-stage');if(card&&stage&&!reduce&&fine){let tx=0,ty=0,cx=0,cy=0,raf=0;stage.addEventListener('pointermove',e=>{const r=stage.getBoundingClientRect();tx=(e.clientX-r.left)/r.width-.5;ty=(e.clientY-r.top)/r.height-.5;if(!raf)raf=requestAnimationFrame(function loop(){cx+=(tx-cx)*.09;cy+=(ty-cy)*.09;card.style.transform=`rotateY(${cx*13}deg) rotateX(${cy*-10}deg) translateZ(10px)`;if(Math.abs(tx-cx)>.001||Math.abs(ty-cy)>.001)raf=requestAnimationFrame(loop);else raf=0})});stage.addEventListener('pointerleave',()=>{tx=0;ty=0;card.style.transform=''})}
if(!reduce&&fine){$$('.tilt').forEach(el=>{let tx=0,ty=0,cx=0,cy=0,raf=0;el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();tx=(e.clientX-r.left)/r.width-.5;ty=(e.clientY-r.top)/r.height-.5;if(!raf)raf=requestAnimationFrame(function loop(){cx+=(tx-cx)*.12;cy+=(ty-cy)*.12;el.style.transform=`perspective(1100px) rotateX(${cy*-3.4}deg) rotateY(${cx*4.4}deg) translateY(-4px)`;if(Math.abs(tx-cx)>.001||Math.abs(ty-cy)>.001)raf=requestAnimationFrame(loop);else raf=0})});el.addEventListener('pointerleave',()=>{tx=0;ty=0;el.style.transform=''})})}
const glow=$('.cursor-glow');if(glow&&fine&&!reduce){let x=innerWidth/2,y=innerHeight/2,tx=x,ty=y;addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY},{passive:true});const loop=()=>{x+=(tx-x)*.045;y+=(ty-y)*.045;glow.style.left=`${x}px`;glow.style.top=`${y}px`;requestAnimationFrame(loop)};requestAnimationFrame(loop)}
const heroCopy=$('.hero-copy');if(heroCopy&&fine&&!reduce){let px=0,py=0,tx=0,ty=0;addEventListener('pointermove',e=>{tx=(e.clientX/innerWidth-.5)*2;ty=(e.clientY/innerHeight-.5)*2},{passive:true});const loop=()=>{px+=(tx-px)*.025;py+=(ty-py)*.025;heroCopy.style.transform=`translate3d(${px*-4}px,${py*-3}px,0)`;requestAnimationFrame(loop)};requestAnimationFrame(loop)}
const tabs=$$('.company-tab'),cards=$$('.company-card');const filter=value=>{tabs.forEach(t=>{const active=t.dataset.company===value;t.classList.toggle('active',active);t.setAttribute('aria-selected',String(active))});cards.forEach((c,i)=>{const show=value==='all'||c.dataset.company===value;c.classList.toggle('is-hidden',!show);if(show&&!reduce)c.animate([{opacity:0,transform:'translateY(22px) scale(.97)'},{opacity:1,transform:'none'}],{duration:650,delay:i*70,easing:'cubic-bezier(.16,1,.3,1)',fill:'both'})})};tabs.forEach(t=>t.addEventListener('click',()=>filter(t.dataset.company||'all')));tabs.forEach((t,i)=>t.addEventListener('keydown',e=>{if(!['ArrowRight','ArrowLeft','Home','End'].includes(e.key))return;e.preventDefault();let n=i;if(e.key==='ArrowRight')n=(i+1)%tabs.length;if(e.key==='ArrowLeft')n=(i-1+tabs.length)%tabs.length;if(e.key==='Home')n=0;if(e.key==='End')n=tabs.length-1;tabs[n].focus();tabs[n].click()}));
$$('.btn').forEach(btn=>{if(!fine||reduce)return;btn.addEventListener('pointermove',e=>{const r=btn.getBoundingClientRect();const x=(e.clientX-r.left-r.width/2)*.08,y=(e.clientY-r.top-r.height/2)*.08;btn.style.transform=`translate(${x}px,${y}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')});
const year=$('#year');if(year)year.textContent=new Date().getFullYear();})();